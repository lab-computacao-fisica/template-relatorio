\addcontentsline{toc}{section}{Referências}

% Desconsidere isso no seu texto. Apenas para fins didáticos:

% Referências devem ser escritas no arquivo "main.bib" no diretório do seu Overleaf, se estiver usando localmente, acesse o caminho, exemplo:

% Se a referência estiver no mesmo diretório do seu main.tex, apenas use "./main.bib" ou o nome que quiser dar para o arquivo.

% Para a referências aparecer, você deve citá-las no texto com o código de identificação da referência no main.bib e \cite{referencia}. 

% Por fim, use o \printbibliography na parte desejada do texto.

\printbibliography